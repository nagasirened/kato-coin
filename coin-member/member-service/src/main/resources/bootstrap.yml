spring:
  application:
    name: member-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: nacos-server:8848
      config:
        file-extension: yaml
        # 拉取配置文件的dataID: member-service-dev.yaml